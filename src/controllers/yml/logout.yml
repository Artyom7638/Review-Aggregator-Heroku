Выход из системы
  ---
  produces:
    - text/html
  responses:
    302:
      description: |-
        Если пользователь авторизован, осуществляет выход из системы и перенаправляет на главную страницу.
        Если пользователь не авторизован, перенаправляет на страницу авторизации.
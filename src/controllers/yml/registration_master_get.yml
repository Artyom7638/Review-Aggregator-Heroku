Страница регистрации мастера
  ---
  produces:
  - text/html

  responses:
    302:
      description: |-
        Перенаправляет на главную страницу если пользователь авторизован.
    200:
      description: |-
        Возвращает страницу с формой для регистрации мастера.
      content:
        text/html:
          schema:
            type: string